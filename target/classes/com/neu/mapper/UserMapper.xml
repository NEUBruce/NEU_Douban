<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.neu.mapper.UserMapper">
    <!--    无法识别时使用ResultMap-->
    <resultMap id="userResultMap" type="com.neu.pojo.User">
        <result column="id" property="userId"/>
        <result column="username" property="username"/>
        <result column="password" property="password"/>
        <result column="gender" property="gender"/>
        <result column="vocation" property="vocation"/>
        <result column="zip_code" property="zipCode"/>
        <result column="age" property="age"/>
    </resultMap>
    <select id="selectAllUser" resultMap="userResultMap">
        select * from user
    </select>

    <select id="selectUserById" resultMap="userResultMap" parameterType="com.neu.pojo.User">
        select * from user where id=#{userId}
    </select>

    <insert id="insertUser" parameterType="com.neu.pojo.User">
        insert into user values(#{userId}, #{username}, #{password}, #{gender}, #{vocation},#{zipCode},#{age})
    </insert>

    <update id="modifyUser">
        update user set id=#{user_id}, userName=#{username}, password=#{password},gender=#{gender},vocation=#{vocation}, zip_code=#{zipCode},age=#{age}
    </update>

    <delete id="deleteUserByName">
        delete from user where username=#{username}
    </delete>

    <select id="login" resultMap="userResultMap">
        select * from user where username=#{username}
    </select>

</mapper>